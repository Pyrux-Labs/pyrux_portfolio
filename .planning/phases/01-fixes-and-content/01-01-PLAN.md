---
phase: 01-fixes-and-content
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - pyrux_portfolio/data/faq.ts
  - pyrux_portfolio/data/packages.ts
  - pyrux_portfolio/data/technologies.ts
  - pyrux_portfolio/app/pricing/page.tsx
autonomous: true
requirements: [FAQ-01, FAQ-02, FAQ-03, FAQ-04, FAQ-05, FAQ-06, DATA-01, DATA-02, DATA-03, A11Y-04]

must_haves:
  truths:
    - "FAQ answer for 'Que necesito tener listo' reads exactly 'Un manual de marca hecho por un profesional seria ideal, pero no es obligatorio.'"
    - "FAQ answer for 'Puedo actualizar el contenido' mentions 'plan Standard' not 'plan Pro'"
    - "FAQ answer for 'Que pasa si no quiero contratar mantenimiento' reads exactly 'Te entregamos la aplicacion lista, para que los realices por tu cuenta.'"
    - "FAQ answer for 'Por que Pyrux y no una agencia' already matches required copy (verify only)"
    - "All maintenance backup cards across all tiers say 'Intervalo de copias dependiente de tu plan.'"
    - "Monitoring card descriptions already correct across all tiers (verify only)"
    - "Exactly 10 technologies have featured: true"
    - "5 new technologies added with featured: false"
    - "No 'USD' string appears in any visible pricing content -- all replaced with 'dolares'"
    - "Zero console.log statements exist in codebase (already clean -- verify only)"
  artifacts:
    - path: "pyrux_portfolio/data/faq.ts"
      provides: "Updated FAQ answers for 3 questions"
      contains: "plan Standard en adelante"
    - path: "pyrux_portfolio/data/packages.ts"
      provides: "Unified backup descriptions, currency label updates"
      contains: "Intervalo de copias dependiente de tu plan"
    - path: "pyrux_portfolio/data/technologies.ts"
      provides: "Corrected featured flags, 6 new technology entries"
      contains: "google-search-console"
    - path: "pyrux_portfolio/app/pricing/page.tsx"
      provides: "Updated metadata with dolares instead of USD"
      contains: "Precios en dolares"
  key_links:
    - from: "pyrux_portfolio/data/faq.ts"
      to: "pyrux_portfolio/app/pricing/PricesPageClient.tsx"
      via: "faqItems import"
      pattern: "import.*faqItems.*from.*faq"
    - from: "pyrux_portfolio/data/packages.ts"
      to: "pyrux_portfolio/app/pricing/PricesPageClient.tsx"
      via: "packages import"
      pattern: "import.*packages.*from.*packages"
    - from: "pyrux_portfolio/data/technologies.ts"
      to: "pyrux_portfolio/components/sections/OurStack.tsx"
      via: "technologies import"
      pattern: "import.*technologies.*from.*technologies"
---

<objective>
Update all static content: FAQ answers, maintenance card descriptions, technology featured flags, new technology entries, and currency labels from USD to dolares.

Purpose: Every visible string on the pricing page and FAQ section must match the latest business messaging. The technology showcase must feature the correct 10 tools.
Output: Updated data files (faq.ts, packages.ts, technologies.ts) and pricing page metadata (page.tsx).
</objective>

<execution_context>
@/home/littlepants/.claude/get-shit-done/workflows/execute-plan.md
@/home/littlepants/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/01-fixes-and-content/01-CONTEXT.md
@.planning/phases/01-fixes-and-content/01-RESEARCH.md

<interfaces>
<!-- Data types used by data files -->
From pyrux_portfolio/types/pricing.types.ts:
```typescript
export interface FAQItem {
  question: string;
  answer: string;
}

export interface MaintenanceItem {
  icon: string;
  title: string;
  description: string;
}
```

From pyrux_portfolio/types/index.ts:
```typescript
export interface Technology {
  id: string;
  name: string;
  icon: string;
  category: "frontend" | "backend" | "database" | "devops" | "other";
  featured: boolean;
}
```
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Update FAQ answers and pricing currency labels</name>
  <files>pyrux_portfolio/data/faq.ts, pyrux_portfolio/data/packages.ts, pyrux_portfolio/app/pricing/page.tsx</files>
  <read_first>
    - pyrux_portfolio/data/faq.ts (current FAQ answers to compare)
    - pyrux_portfolio/data/packages.ts (current price strings and backup descriptions)
    - pyrux_portfolio/app/pricing/page.tsx (metadata with USD strings)
    - .planning/phases/01-fixes-and-content/01-CONTEXT.md (exact copy to use)
  </read_first>
  <action>
**In pyrux_portfolio/data/faq.ts:**

1. FAQ-01: Find the question containing "Que necesito tener listo antes de empezar" and replace its answer with exactly:
   `"Un manual de marca hecho por un profesional seria ideal, pero no es obligatorio."`
   (Note: current text has extra sentence "Con textos, logo e imagenes basicas es suficiente para arrancar." which must be removed.)

2. FAQ-02: Find the question containing "Puedo actualizar el contenido yo mismo" and replace its answer with exactly:
   `"El plan Standard en adelante incluyen una aplicacion web para que puedas cambiar el contenido por vos mismo; mientras que en el plan Growth van por el mantenimiento mensual."`
   (Note: current text says "Plan Pro" -- must change to "plan Standard". Current says "plan Growth va" -- must change to "plan Growth van".)

3. FAQ-03: Find the question containing "Que pasa si no quiero contratar mantenimiento" and replace its answer with exactly:
   `"Te entregamos la aplicacion lista, para que los realices por tu cuenta."`
   (Note: current text is missing the comma after "lista" -- add it.)

4. FAQ-04: Verify the answer for "Por que Pyrux y no una agencia?" already matches: "Las agencias cobran un 200% mas que nuestros precios, y tercerizan tu proyecto a un programador inexperto. En Pyrux, aseguras que hablas directamente con los profesionales que realizaran tu proyecto, manteniendo una calidad y personalizacion que no vas a tener en ninguna otra empresa." -- NO CHANGE NEEDED, already correct.

5. DATA-03 (faq.ts): In the answer for "Los precios son en dolares o en pesos?", change `"Se cotizan en USD"` to `"Se cotizan en dolares"`.

**In pyrux_portfolio/data/packages.ts:**

6. FAQ-06: Update ALL backup card descriptions. Find every object with `icon: "HardDrive"` and change its `description` to exactly: `"Intervalo de copias dependiente de tu plan."`
   - growthMaintenanceCards (line ~22): currently "Intervalo de copias cada 14 dias" -- CHANGE
   - proMaintenanceCards (line ~55): currently "Intervalo de copias cada 7 dias" -- CHANGE
   - businessMaintenanceCards (line ~88): currently "Intervalo de copias cada 7 dias" -- CHANGE

7. FAQ-05: Verify all monitoring cards (icon: "SatelliteDish") already say "Arreglamos los problemas antes de que el usuario los vea" -- NO CHANGE NEEDED if already correct.

8. DATA-03 (packages.ts): Change all USD price strings:
   - `"USD 300"` -> `"300 dolares"`
   - `"USD 500"` -> `"500 dolares"`
   - `"Desde USD 800"` -> `"Desde 800 dolares"`
   - `"USD 800"` -> `"800 dolares"`
   - `"USD 2.000"` -> `"2.000 dolares"`

**In pyrux_portfolio/app/pricing/page.tsx:**

9. DATA-03 (metadata): Change both occurrences of `"Precios en USD"` to `"Precios en dolares"` in the `description` and `openGraph.description` fields.
  </action>
  <verify>
    <automated>cd /home/littlepants/Documents/repositories/pyrux_portfolio/pyrux_portfolio && grep -c "USD" data/faq.ts data/packages.ts app/pricing/page.tsx | grep -v ":0$" | wc -l | grep "^0$" && grep "plan Standard" data/faq.ts && grep -c "Intervalo de copias dependiente de tu plan" data/packages.ts | grep "^3$" && grep "dolares" app/pricing/page.tsx | head -1</automated>
  </verify>
  <acceptance_criteria>
    - `grep "USD" pyrux_portfolio/data/faq.ts` returns 0 matches
    - `grep "USD" pyrux_portfolio/data/packages.ts` returns 0 matches
    - `grep "USD" pyrux_portfolio/app/pricing/page.tsx` returns 0 matches
    - `grep "plan Standard" pyrux_portfolio/data/faq.ts` returns 1 match
    - `grep -c "Intervalo de copias dependiente de tu plan" pyrux_portfolio/data/packages.ts` returns 3
    - `grep "dolares" pyrux_portfolio/app/pricing/page.tsx` returns at least 1 match
    - `grep "300 dolares" pyrux_portfolio/data/packages.ts` returns 1 match
    - `grep "500 dolares" pyrux_portfolio/data/packages.ts` returns 1 match
    - `grep "Desde 800 dolares" pyrux_portfolio/data/packages.ts` returns 1 match
    - FAQ-04 answer still contains "Las agencias cobran un 200%" (unchanged)
  </acceptance_criteria>
  <done>All FAQ answers match exact required copy. All backup descriptions unified. All USD replaced with dolares. Monitoring cards verified correct.</done>
</task>

<task type="auto">
  <name>Task 2: Update technology featured flags and add new entries</name>
  <files>pyrux_portfolio/data/technologies.ts</files>
  <read_first>
    - pyrux_portfolio/data/technologies.ts (current technology entries and featured flags)
    - pyrux_portfolio/types/index.ts (Technology type definition)
    - .planning/phases/01-fixes-and-content/01-CONTEXT.md (exact list of featured technologies)
  </read_first>
  <action>
**In pyrux_portfolio/data/technologies.ts:**

1. Set `featured: false` for these 6 existing entries (currently `featured: true`):
   - `id: "react"` (line ~14)
   - `id: "javascript"` (line ~35)
   - `id: "express"` (line ~79)
   - `id: "postgresql"` (line ~95)
   - `id: "docker"` (line ~118)
   - `id: "git"` (line ~125)

2. Set `featured: true` for these 5 existing entries (currently `featured: false`):
   - `id: "supabase"` (line ~176)
   - `id: "vercel"` (line ~147)
   - `id: "linux"` (line ~139)
   - `id: "github"` (line ~131)
   - `id: "figma"` (line ~170)

3. Add 1 new featured entry at the end of the "other" category section (before the closing bracket of the array):
```typescript
{
  id: "google-search-console",
  name: "Google Search Console",
  icon: "SiGooglesearchconsole",
  category: "other",
  featured: true,
},
```

4. Add 5 new non-featured entries at the end of their respective category sections:

In the frontend section (after css entry):
```typescript
{
  id: "vite",
  name: "Vite",
  icon: "SiVite",
  category: "frontend",
  featured: false,
},
{
  id: "threejs",
  name: "Three.js",
  icon: "SiThreedotjs",
  category: "frontend",
  featured: false,
},
```

In the backend section (after python entry):
```typescript
{
  id: "django",
  name: "Django",
  icon: "SiDjango",
  category: "backend",
  featured: false,
},
```

In the devops section (after nginx entry):
```typescript
{
  id: "github-pages",
  name: "GitHub Pages",
  icon: "SiGithubpages",
  category: "devops",
  featured: false,
},
{
  id: "render",
  name: "Render",
  icon: "SiRender",
  category: "devops",
  featured: false,
},
```

5. After all changes, the featured count must be exactly 10: Next.js, TypeScript, Tailwind CSS, Node.js, Supabase, Vercel, Linux, GitHub, Figma, Google Search Console.

6. A11Y-04: Verify zero `console.log` statements exist in the codebase. Research confirmed already clean -- just run the grep to confirm. No changes expected.
  </action>
  <verify>
    <automated>cd /home/littlepants/Documents/repositories/pyrux_portfolio/pyrux_portfolio && node -e "const {technologies}=require('./data/technologies.ts'.replace('.ts','')); console.log(technologies.filter(t=>t.featured).length)" 2>/dev/null || grep -c "featured: true" data/technologies.ts | head -1 && grep "google-search-console" data/technologies.ts && grep "SiVite" data/technologies.ts && grep -rc "console.log" app/ components/ data/ hooks/ | grep -v ":0$" | wc -l</automated>
  </verify>
  <acceptance_criteria>
    - `grep -c "featured: true" pyrux_portfolio/data/technologies.ts` returns exactly 10
    - `grep "google-search-console" pyrux_portfolio/data/technologies.ts` returns 1 match
    - `grep "SiGooglesearchconsole" pyrux_portfolio/data/technologies.ts` returns 1 match
    - `grep "SiVite" pyrux_portfolio/data/technologies.ts` returns 1 match
    - `grep "SiThreedotjs" pyrux_portfolio/data/technologies.ts` returns 1 match
    - `grep "SiGithubpages" pyrux_portfolio/data/technologies.ts` returns 1 match
    - `grep "SiDjango" pyrux_portfolio/data/technologies.ts` returns 1 match
    - `grep "SiRender" pyrux_portfolio/data/technologies.ts` returns 1 match
    - `grep "featured: true" pyrux_portfolio/data/technologies.ts` does NOT match react, javascript, express, postgresql, docker, or git entries
    - `grep -rc "console.log" pyrux_portfolio/app/ pyrux_portfolio/components/ pyrux_portfolio/data/ pyrux_portfolio/hooks/ | grep -v ":0$"` returns 0 lines
  </acceptance_criteria>
  <done>Exactly 10 technologies featured (correct set). 6 new technology entries added (1 featured, 5 not). Zero console.log in codebase confirmed.</done>
</task>

</tasks>

<verification>
After both tasks:
1. `cd pyrux_portfolio && npm run build` must exit 0 (static export succeeds)
2. Zero `grep -r "USD" data/ app/pricing/page.tsx` matches
3. `grep -c "featured: true" data/technologies.ts` returns 10
4. `grep -c "Intervalo de copias dependiente de tu plan" data/packages.ts` returns 3
</verification>

<success_criteria>
- All 4 FAQ answers match exact copy from CONTEXT.md
- All backup card descriptions unified across all tiers
- All USD references replaced with dolares
- Exactly 10 featured technologies (correct set)
- 6 new technology entries present
- Build succeeds with zero errors
</success_criteria>

<output>
After completion, create `.planning/phases/01-fixes-and-content/01-01-SUMMARY.md`
</output>
